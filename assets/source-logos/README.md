# Source logo artwork

This directory keeps the original high-resolution logo files used to generate web-ready assets.
Run `npm run assets:build` after replacing a source file. Generated, optimized images are written
to `static/logos/` and are the files referenced by the website.
